# Agents library implementation report

This candidate implements feature `wi_a8f6e4e08f3a1cef` revision 2 under bounded work-order message `tsk_2cfcff70a0fbe967#1720`, from baseline `4e8e6c5b38905b25b5173d0044560be45e5b6e88`. It is an implementation candidate only: integration, deployment, production profile migration, release acceptance, and lifecycle closure need later orders.

## Data contract

- Plaintext vault/profile data uses Agents catalog version 2 and Teams schema version 2. A catalog contains at most 1,000 stable definitions; up to 30 teams each contain 1–32 references. Definitions retain their own revision and all launch fields. Team members contain only the definition ID plus optional alias and role overrides.
- Legacy embedded team members migrate one-for-one to separate definitions before legacy filtering. There is no name/content deduplication. Migration is strict and atomic: duplicate/invalid/overflow/future-version content rejects the operation, explicit `teams: []` remains empty, and the exact original v1 encrypted envelope is copied once into an immutable recovery record in the same transaction that commits v2. The encrypted recovery envelope remains explicitly exportable while locked or when the selected v2 record is missing; updated code never uses it as a silent active-data fallback.
- Agent edits use definition-revision comparison. Referenced definitions and machines used by unresolved plans cannot be deleted. Example customization atomically adds copied definitions and a referenced team.

## Compatibility and encryption

- New local data is encrypted as a v2 AES-GCM envelope in `encrypted-v2`. A single IndexedDB transaction writes the immutable `migration-source-v1` record (on first migration), current v2 record, and v2 selection pointer. The mutable old `encrypted` record is also retained for old software; once the pointer exists, updated code never silently falls back to or merges old-namespace writes.
- Vault v1 and v2 keep PBKDF2-SHA256/600,000 and use versioned AAD. Profile v1 and v2 keep the original PBKDF2/HKDF salts/info, so profile authentication tokens and encryption-key derivation do not change. Profile envelopes use versioned AAD.
- The hub profile service advertises service version 2 and envelope versions 1/2. A valid revision CAS is checked before its atomic monotonic version fence: a stored v2 profile cannot be overwritten by v1, and rejected writes do not alter the current record or history.
- An updated client blocks profile sync to a service that cannot advertise the v2 contract. Old clients cannot edit v2 data. Rollback means using the retained legacy snapshot with old software; it does not mean downgrading or merging the current v2 profile.

## Launch snapshots and retries

- Team references resolve to copied launch fields before project/agent effects. Copies retain definition ID/revision provenance, exact work-item/order/context identity, independent reasoning/permission fields, and preallocated agent identity.
- The local encrypted retry journal is non-portable and capped at eight plans, 512 KiB per plan, and 2 MiB total. Shared work-item context is stored once per plan; server credentials are not stored. Capacity is checked before project/agent effects.
- A member becomes `uncertain` before its host request. Retry reconciles the exact preallocated agent ID and resulting run before proceeding. It never treats an unknown response as unstarted. A host-verified pre-registration failure can return to `unstarted`. Retry controls are rebuilt from those frozen member IDs, never from the current shared definition; only a deliberate folder edit for that exact verified-unstarted identity is copied back with a receipt.
- Reconciliation validates the frozen project, identity, known run, lifecycle, role, parent, runtime, working directory, and complete work-item/order/context binding before considering name conflicts or launching another member. An exact registered member whose reply was lost is retained and the remaining members continue without a second spawn.
- Only an `unstarted` member can receive a deliberate folder amendment. Its machine and all definition-derived settings remain frozen, and old/new folder provenance is retained. Started and uncertain identities remain unchanged. Unresolved add-team and new-project plans can be restored after reload only for the same encrypted hub token/profile instance and exact machine endpoint/credential generation. Scope and refresh-epoch guards run before and after every asynchronous launch/reconciliation effect, so a delayed result cannot continue through a changed client, profile, endpoint, or credentials.
- Before a team project is created, the journal saves the exact create request and complete set of existing task IDs, then marks the attempt uncertain before the POST. The submit single-flight fence is set synchronously before preparation or journal awaits. A lost response blocks every further create and launch: project field equality—even one new exact-field match—is not an authoritative creation receipt and is never adopted. The same fence survives reload. Explicit scope-guarded discard is available only after manual inspection; seamless recovery requires a separately reviewed idempotency/receipt API.

## Runtime controls

Reasoning, Codex approval policy, Codex sandbox, Claude permission mode, and Claude allowed-tool rules are independent fields. Inherit emits no corresponding runtime override. Unsupported explicit combinations are rejected in the UI/shared command builder and again in `tt spawn` before hub/provider effects.

The advertised Codex reasoning matrix is the documented intersection of `low`, `medium`, `high`, and `xhigh` for [GPT-5.3-Codex](https://developers.openai.com/api/docs/models/gpt-5.3-codex), [GPT-6 Astra](https://developers.openai.com/api/docs/models/gpt-6-astra), [GPT-5.6 Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol), [GPT-5.6 Terra](https://developers.openai.com/api/docs/models/gpt-5.6-terra), and [GPT-5.6 Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna). The [Codex configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference) documents `model_reasoning_effort` as `minimal | low | medium | high | xhigh`, with `xhigh` model-dependent. Tailterm does not advertise API-only `none` or `max`, or `minimal`, because they are not in that exact common supported set. Unknown/custom/default models and every custom command wrapper are inherit-only; explicit reasoning is accepted only when the original run command is exactly the verified native `codex` executable. A bounded local `codex --version`/`codex --help` inspection observed `codex-cli 0.153.4` and the supported `-c`, `--sandbox`, and `--ask-for-approval` argv surfaces. No account, authentication, provider, installation, configured-MCP, or generation probe was performed.

GPT-6 Sol (`gpt-6-sol`) and GPT-6 Luna (`gpt-6-luna`) join the same Codex matrix (September 23, 2026), per the [Codex models page](https://learn.chatgpt.com/docs/models). Live check that day: `codex-cli` 0.154.0 did not list `gpt-6-sol`, and the server rejected it for the ChatGPT-account login. After upgrading the npm global `@openai/codex` to 0.156.1, the catalog listed `gpt-6-sol` (low–ultra) and `gpt-6-luna`. A real `codex exec -m gpt-6-sol` call at `medium` succeeded, and the `codex queue --help` surface the relay uses was unchanged.

Claude Code accepts `--effort low|medium|high|xhigh|max`, and Tailterm passes it for exact model IDs only: `claude-opus-5-5`, `claude-fable-5-1`, `claude-opus-5` and `claude-sonnet-5` all support all five levels. Aliases (`opus`, `sonnet`, `opusplan`) follow the server's configuration and stay inherit-only. Haiku 4.5 has no effort control. As with Codex, explicit effort requires the exact native `claude` run command. A command override that already contains `--effort` is rejected. Verified against installed Claude Code 2.1.281 `--help`.

## Verification and rollback

Synthetic-only verification covers migration, strict stable v2 persisted IDs/revisions, unchanged encrypted storage after rejected unlock/import, reference sharing, stale revision/deletion fences, envelope compatibility/tampering, immutable locked recovery after stale old-client writes and interrupted v2 migration, profile sync/conflict/recovery/downgrade, journal bounds/context deduplication, native-command reasoning rejection, command argv, project-handler propagation, partial launch retry, and realistic long-content layout in Chromium and WebKit. The task-form integration uses an isolated real Go hub, real `tt`/tmux registrations, and the encrypted IndexedDB journal to cover a journal write failure before effects; committed-but-lost creation blocked in the same dialog and after reload; an uncommitted create followed by an unrelated sole exact-field project; simultaneous submit single-flight; live definition name/runtime/default-folder edits across same-dialog and reloaded retry; exact identity-scoped folder amendment receipts; registered-but-lost worker responses; delayed token/profile-generation/endpoint-credential changes across an in-flight host command; continuation without duplicate identities; and run/lifecycle/work-binding mismatches. WebKit results are not represented as native Safari results.

Rollback before release is to omit this candidate. After v2 local migration, an old build can use only the retained v1 snapshot; it must not be presented as current or merged. Hub rollback while current v2 profiles exist is unsafe unless the writer remains v2-aware. The v2 pointer and current v2/profile records should not be deleted unless the owner explicitly chooses recovery and accepts losing newer data.

Known verification limitations: the broad `hub/cmd/tt` test package contains a pre-existing coordination-test HTTP connection leak/hang (`TestPostHumanReplyAndLiteralHelp`). Focused changed CLI tests pass, and all other Go packages pass. After an isolated `npm ci` supplied the lockfile dependencies, both ordinary and static production builds completed; npm reported one deprecated dependency, three moderate audit findings, and six install scripts not allowlisted by npm's script policy. No manifest was changed and no audit fix or script approval was run. No deployment, provider execution, live profile/task access, or production migration was performed.

## Actual release — September 10, 2026

Lead actual-release order `#1840` extended original implementation `#1720` and preparation `#1821` for the same feature revision/scope, worker, run, context, and active Queue binding. The database handler committed and independently read back that exact order in `#1844/#1845` as `nart_ace4966a8b519ca5` v1, receipt `nrr_65d7b1a1872874f6`. Lead message `#1824` is correctly attributed to lead agent `agt_1186df8d710c0fd7`; it directed the handler to record preparation order `#1821` and explicitly did not authorize deployment. Production deployment authority came later and only from `#1840`.

The released integrated application is exact tracked-clean commit `22ab0005795c9a1201766159e7b910fcff8e2a03`, retained at `.build/releases/agents-library-22ab000`. It combines accepted Agents `e6e927b045931210ca3702c3c0c70a2e39330b4f`, including the narrow-layout correction, with accepted Queue layout `a974c9870b64d0e4cb5387011d1a68515af6ce82`. The exact static manifest is SHA-256 `bd9c2ac5753ec8455f3c85ce5a94f5fe6d4955d049e57ce210ca59204dd886f3`, 82 entries / 80,169,986 bytes. Production WASM is `fed7a07b5767f9c4a288791e87dbb2e4e466b48e3f45ad5c606e6d10cec7f804`; the matching 305-entry module inventory is `544ac403338ecbcbd74f0e357f4f99a53c8120f990330c772fc51fdd6f1272ce`.

Before deployment, the handler used SQLite's online backup API against a read-only source while the hub remained running. The mode-0600 backup is `/mnt/deepfreeze/tailterm-hub/backups/before-agents-library-20260911T003758289138Z.sqlite`, 17,047,552 bytes, SHA-256 `92f1445fdab9143bef79b355f7d7f630c820490a615b8b04cdd63d96389943f0`. Both backup and live `integrity_check` returned `ok`, with zero foreign-key violations. Handler artifact `nart_e3c28bdbb1a53ad7` v1 / receipt `nrr_45e8223278cda3d3` preserves the receipt. It is disaster recovery only, never an instruction to overwrite newer work.

The existing TrueNAS app was updated through middleware to unique immutable release `20260910-agents-library-22ab000`. Its Linux amd64 binary is 27,226,274 bytes, SHA-256 `4c093f2936985d4031bb3a5c834ed991a233a0f0215599b07a3b8f053dac20a6`. The app remains one running distroless/nonroot container with UID/GID 950:950, private listener `100.116.238.37:18765`, read-only binary/token mounts, and the same read-write state mount. The profile service reports `tailterm-profiles` v2, instance `profilehub_67c148b93820fd03`, envelope versions 1/2, and minimum write envelope version 2. Queue v1, message audit v1/v2, audit export v2/v3, and observe policy remain advertised. Authenticated doctor passed and unauthenticated `whoami` remained 403.

Mini and Air atomically installed the same Darwin arm64 CLI, 6,698,210 bytes, SHA-256 `465498d97465994aaecdfb44ba9c7725b55dc5858547495ed0bcfaffc8d9346d`. Each host retains `tt-before-agents-library-22ab000`, SHA-256 `ec8bf996bbde9761563ed67c200c65641d8769aa1f504843653b0980a671d493`. Doctor passed on both and installed help exposes independent reasoning, approval, and sandbox flags. No provider or agent launch was used. Mini relay PID 83457 / PPID 1 and Air relay PID 912 / PPID 1 were unchanged.

Wrangler deployed only the exact `dist-static` package to Pages project `tailos`, production branch `main`, from commit `22ab000…` with `dirty=false`. Deployment `53780e9d-bd87-43e9-8b21-a59c62b43430` is `https://53780e9d.tailos.pages.dev`; the custom domain is `https://tailos.tailarr.com`. The old `tailterm` project was not used. Mini's prior a974c98 live package first matched its retained copy byte-for-byte, then macOS `renameatx_np(RENAME_SWAP)` atomically exchanged a verified clone-copy of the new package with the live directory. The prior live directory is retained at `.build/static-live-before-agents-library-a974c98-swap`. Preview PID 60799 / PPID 1 and port 4318 were not stopped, restarted, or reparented. Air's webpage preview remains excluded.

All three origins returned the exact release manifest, canonical root index, and 81/81 served assets with expected size, SHA-256, status, MIME contract, and no unexpected content encoding. Evidence `.build/releases/agents-library-22ab000/.build/agents-library-release/public-assets.json` is 71,353 bytes, SHA-256 `954527a5c3ad79253d9c8444874b821ff5c83bc8fd19fd754b744f49d2728d9f`. Fresh disposable Chromium contexts on every origin started production WASM, generated/restored only a synthetic vault key, retained expected margins, and reported no page errors. The test's hard-coded `https:true` field is not an HTTPS claim for Mini's explicit `http://127.0.0.1:4318` origin.

The stylesheet downloaded from each origin was byte-identical to production SHA-256 `207a3460df8db8635ac0810725120706bd661291f7341803c431d6cac127a8bf`. Each copy passed the full Agents and Teams long-content geometry/interaction matrix in Chromium and WebKit at 320/390/680/760/761/1120 CSS pixels. Each also passed all 24 Queue Chromium/WebKit layout scenarios. Queue result hashes are `aa6b4da4f66aa4c45225fbbe3548fd6ec14306ea6bd2429b08c526bb043d67ac` (immutable), `a5b0f484e12e32f86c94c141f0c0a1d60dfc235986c30e6e7d7c359c64a79c0b` (custom), and `94bbdc001304d3723caee51e6f20e953d632ad497bc6e9fb5db2021d01fd5650` (Mini). WebKit is not native Safari or owner-device confirmation.

Operational corrections were non-product verifier issues. An initial shell check used zsh's reserved `status` variable after doctor had passed; the unauthenticated check was immediately rerun with `http_code` and returned 403. The first asset verifier MIME table omitted `.mjs`, then assumed Mini mapped it like Pages; the final complete rerun accepts Pages JavaScript and Mini's established octet-stream fallback. Cloudflare Web Analytics injects a beacon into custom-domain HTML for ordinary browser-like responses, so exact stored-byte verification deliberately requested `Accept: application/octet-stream` plus identity encoding; normal Chromium still passed with the served response. No deployed file was changed for these corrections.

Retained implementation limitations remain: the broad static test fixture still uses obsolete `Task` labels and omits the required first-agent folder, the broad `hub/cmd/tt` package retains its reported coordination-test hang, npm reports three moderate advisories/six blocked install scripts, and Vite reports its existing large-chunk warning. No live owner profile/vault/task was used as a fixture and no profile migration was forced. No Tailscale, DNS, network, relay, Air-preview, unrelated service, agent, task, Queue, or worker-lifecycle change occurred.

Before any v2 write, prior Queue frontend/Project Queue hub/host CLI copies remain conditional rollback points only if no new launch intent depends on them. Once v2 could have been written, retain this v2-readable hub/frontend and current CLI, or deploy a separately reviewed v2-compatible forward correction. Never delete v2 pointers/envelopes/history, merge legacy data into current v2, downgrade to a v1-only writer, or blindly restore the pre-release database.
