# Overnight release: `cf80b46`

Seven items accepted on the board overnight on September 24–25, 2026, released as one stack on
the morning of September 25. The owner approved each outward step (push, hub and bridge, Mini
`tt`, TailOS) in the owner's Claude Code session. The overnight timeline and the owner decisions
made on the owner's behalf are in `.build/overnight-2026-09-24.md`.

The Discord server, application and owner IDs are redacted from the plan and receipt copies
here. The unredacted files are in `.build/overnight-release-cf80b46/`, and the pinned receipt
SHA-256 (`d9f88ab5…`) is of the unredacted receipt.

## Scope
All in the shadow-week project (`tsk_e7af3c28a444b09a`), one team at a time.

| # | Item | Accepted head | What it does |
|---|---|---|---|
| 1 | Projects Page UI (`wi_44c011464493e95e`) | `d1c98df` | Collapsible team list, no responsibility note, quiet saves |
| 2 | Reply wake (`wi_1feb0a47cabb6e45`) | `4c43e77` | An unaddressed agent reply goes to the live original author |
| 3 | `tt withdraw` (`wi_0e5593cf1787292a`) | `870f15e` | A sender's run withdraws its own superseded open request |
| 4 | `tt team launch` (`wi_9a1ab2f3c0689db3`) | `6bdb639` | Launch a Planned delivery team from the CLI, same plan as TailOS |
| 5 | `tt close --team` (`wi_5072afd345d50fd5`) | `3d312a1` | A lead closes its team and frees the project when its item is done |
| — | Integration of 1–5 (`wi_90df5e8421f7f988`) | `c1df3f4` | 11 cherry-picks, an integration test and two docs commits on `414aae0` |
| 6 | Queue runner (`wi_f5cdc68a8374e3f8`) | `8df1e99` | Hub-stored project queue; the relay launches, closes and hands off |
| 7 | Green baseline (`wi_4aabfc2484ca1e41`) | `cf80b46` | All Go and npm tests and four browser suites pass |

The committed stack is `414aae0..cf80b46`: 22 commits, 74 files, +9,720 −183.

## Review trail
Every item had its reviews on the board, by Tailterm reviewer agents.
- **Projects UI:** review #9703, lead acceptance #9708, handler save #9707. The release was held
  for the owner (#9709).
- **Reply wake:** two rounds (#9750, #9763) plus a focused b1 fix; lead #9764; handler #9768.
- **`tt withdraw`:** round one #9802 (b1, b2, fixed); round two #9822 passes; lead #9823.
- **`tt team launch`:** round one #9879; round two #9892 passes on an unchanged candidate
  (f1–f8 non-blocking); lead #9894.
- **`tt close --team`:** round one #9925 (b1: a cancel notice blocked closeout; b2: owner path
  and exited lead; both fixed); round two #9945 passes; lead #9946; handler #9949. Owner
  decision #9927: delivery-only obligations held by closing members close as
  `recipient_gone`; substantive obligations still block.
- **Integration:** independent review #10013, no blockers; handler #10019. Every source commit
  cherry-picked with no conflicts ([integration record](../../overnight-integration-2026-09-25.md)).
- **Queue runner:** round one #10078, round two #10099, focused correction verified #10117; lead
  #10118.
- **Green baseline:** accepted; includes a focused first-show recovery fix.

## Verification before release
On a detached worktree at `cf80b46`: `go vet` clean, `go test` with no failures (also with agent
`TAILTERM_*` variables set), `npm test` 215/215, and the hub-cache, board-layout, task-form and
project-compact-ui browser suites pass. board-layout skips 8 heading-position checks, all linked
to `wi_2e5375ae99db3d26` (missing sr-only rule).

The embedded team planner (`hub/internal/teamplan/plan.mjs`, generated by
`scripts/build-team-plan.mjs`) runs under Node.js. The Mini has Node v26.5.1 at
`/opt/homebrew/bin`, which is on the relay LaunchAgent's `PATH`.

## Deploy
1. **Branch:** `tasks-hub` fast-forwarded `414aae0` → `cf80b46` and pushed. All 22 commits carry
   the scs32 noreply identity.
2. **Rollback safety:** the schema change is additive only: new tables `team_queue_entries`,
   `team_queue_requests`, `team_launch_reservations` and `team_close_receipts`, their indexes,
   and a `released_at` column on the new queue table. The integration team verified that the
   `414aae0` hub opens a database migrated by the integrated hub (`c1df3f4`). That check predates
   the queue runner's tables; they are new tables the old hub never reads, but no old-hub run
   against them was recorded.
3. **Backup:** `before-overnight-cf80b46.sqlite` (102,305,792 bytes, SHA-256 `0bf223e1…`).
   Integrity ok, zero foreign-key violations, profile snapshots unchanged from the phase 3.1
   backup. Completed 2026-09-25 15:18:19 UTC.
4. **Hub and bridge:** release `20260925-overnight-cf80b46`, `deployed`/`success`. The TrueNAS app
   is `RUNNING` with the hub and discord-bridge containers running.
   - Hub SHA-256: `25e0c8c5…` (linux/amd64, static, `CGO_ENABLED=0`, `-trimpath`).
   - Bridge SHA-256: `80677406…`.
   - The previous release (`20260924-broker-phase3.1-fdc9b6e`, hub `cea51265…`, bridge
     `cbd608b4…`) remains on TrueNAS; local copies are in `.build/prev-fdc9b6e/`.
5. **Mini `tt`:** `cf80b46`, SHA-256 `b23f9642…`, installed atomically (build to `tt.new`, then
   rename). The rollback binary is `~/.local/bin/tt.prev-414aae0` (`65d19321…`).
   - `tt hooks codex --install` was not needed: `~/.codex/hooks.json` already runs
     `tt hook stop`, and no hook code changed.
   - The relay was restarted with `launchctl kickstart -k` at 08:25:58 PDT and logged nothing
     in the following 45 seconds. `tt doctor` reaches the hub; `tt team queue list` answers from
     the new hub route.
6. **TailOS:** built from clean source; `verify:release` matched all 82 assets to
   `release.json` for `cf80b46`. Deployment `a4628207-8549-4550-813b-3b96b14df590` (Production,
   `main`); `https://tailos.tailarr.com/release.json` reports commit `cf80b46958f7…`,
   `dirty: false`.

## Found during the release
- About 30 seconds of `connection refused` in the relay log during the hub restart, as in
  earlier releases. Afterwards `tt relay --status` still showed that error for all 174
  bindings, because it prints each binding's saved error, and only a successful wake clears it.
  The relay itself was polling normally. Filed as `wi_73940a928adcb5f8` (Tailterm Development),
  delivered through shadow-week `wi_3a2b456f2e997eb4`.
- The bridge containers' logs were not read: `docker logs` on TrueNAS needs a sudo password.

## Live checks
The queue ran nine shadow-week items from 2026-09-25 08:29 to 2026-09-26 (see
[queue releases](../queue-2026-09-25/README.md)).
- **Queue runner hand-off:** works. Each accepted item's lead closed its team, the project lead
  slot cleared, and the runner launched the next entry within seconds. The first hand-off exposed
  one failure: the runner requires the exact item revision recorded at queue time, and the handler
  had bumped two queued items while recording their orders ("queued item changed before launch").
  The entries were released and requeued; handler-save (`cf6b1cf`) removed the cause.
- **`tt close --team`:** works; every accepted team closed itself, clearing the lead and keeping
  the database handler.
- **`tt withdraw`:** used live by an agent (#10758, 2026-09-25).
- **Routed reply waking its author:** not separately confirmed; no stalled hand-off of the
  2026-09-24 kind was seen during the run.
